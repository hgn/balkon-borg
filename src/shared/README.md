# src/shared — cross-cutting contracts

Not started. Will hold the MQTT topic/payload schema once `pi/` and `android/` both
need it as code, not just documentation — currently sketched in
[`../../docs/network.md`](../../docs/network.md).
